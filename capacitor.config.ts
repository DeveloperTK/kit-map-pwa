import {CapacitorConfig} from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'de.foxat.campus',
    appName: 'KIT Campusplan',
    webDir: 'build',
    bundledWebRuntime: false
};

export default config;
